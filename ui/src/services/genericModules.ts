/**
 * Where the shared generic JS modules are served from: this extension's own Polarion webapp context. The dev
 * proxy forwards this absolute path to Polarion, and in Polarion it is same-origin.
 */
export const GENERIC_MODULES_BASE = '/polarion/xml-repair-app/ui/generic/js/modules/';
